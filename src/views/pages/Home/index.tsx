import GuestTable from '@views/components/GuestTable';
import Header from '@views/components/Header';

const Home = () => (
  <section className="w-full h-screen">
    <Header />
    <GuestTable />
  </section>
);

export default Home;
